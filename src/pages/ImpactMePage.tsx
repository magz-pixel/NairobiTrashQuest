import { useCallback, useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { SignInButton } from '../components/auth/SignInButton'
import { RaceTicket3D } from '../components/site/art/RaceTicket3D'
import { SiteFooter } from '../components/site/SiteNav'
import { ActionLink, PageIntro, PublicShell, SectionLabel, StatusChip } from '../components/site/PagePrimitives'
import { useAuth } from '../hooks/useAuth'
import { isSupabaseConfigured, supabase } from '../lib/supabase'
import { AMAZING_TRASH_RACE_S2, type Event, type RaceRegistration } from '../types/database'
import { useCityPath } from '../lib/CityContext'
interface EventRsvp{id:string;event_id:string;status:string;points_awarded:number;events?:Event|null}
const BADGE={scout:'Scout',ranger:'Ranger',guardian:'Guardian'} as const
export function ImpactMePage(){const path=useCityPath();const{user,profile,loading,signOut,refreshProfile}=useAuth();const[tickets,setTickets]=useState<RaceRegistration[]>([]);const[rsvps,setRsvps]=useState<EventRsvp[]>([]);const[mapStats,setMapStats]=useState({reports:0,clears:0});const[busy,setBusy]=useState(false);const load=useCallback(async()=>{if(!user||!isSupabaseConfigured)return;setBusy(true);try{const[{data:regs},{data:rsvpRows},reportsRes,clearsRes]=await Promise.all([supabase.from('race_registrations').select('*').eq('user_id',user.id).eq('event_slug',AMAZING_TRASH_RACE_S2).order('created_at',{ascending:false}),supabase.from('event_rsvps').select('id,event_id,status,points_awarded,events(*)').eq('user_id',user.id).order('created_at',{ascending:false}),supabase.from('reports').select('id',{count:'exact',head:true}).eq('user_id',user.id),supabase.from('reports').select('id',{count:'exact',head:true}).eq('cleared_by',user.id)]);setTickets((regs??[]) as RaceRegistration[]);setRsvps((rsvpRows??[]) as unknown as EventRsvp[]);setMapStats({reports:reportsRes.count??0,clears:clearsRes.count??0});await refreshProfile()}finally{setBusy(false)}},[user,refreshProfile]);useEffect(()=>{void load()},[load]);return <PublicShell><main><PageIntro eyebrow="Your record of showing up" title="My impact, in one place." body="Every report, cleanup, and race ticket connected to your account becomes part of a visible civic record." action={!user&&!loading?<SignInButton variant="dark" label="Join / sign in"/>:user?<div className="flex flex-wrap gap-3"><ActionLink to="/map" tone="gold">Open the map</ActionLink><button type="button" disabled={busy} onClick={()=>void load()} className="fn-action fn-action-light">Refresh record</button></div>:undefined}/><section className="mx-auto max-w-7xl px-5 pb-20 sm:px-8 lg:px-12">{loading&&<p className="text-[#617972]">Loading your record…</p>}{!loading&&!user&&<div className="fn-panel-dark rounded-[2rem] bg-[#063b32] p-8 text-white sm:p-12"><SectionLabel>Join the record</SectionLabel><h2 className="fn-display mt-4 max-w-xl text-4xl font-extrabold tracking-[-.05em]">Your next small action can count.</h2><p className="mt-4 max-w-lg leading-7 text-teal-100/75">Create an account to save reports, cleanup attendance, and your race ticket.</p><div className="mt-7"><SignInButton variant="dark" label="Join free"/></div></div>}{user&&<div className="space-y-8"><section className="fn-panel-dark rounded-[2rem] bg-[#063b32] p-7 text-white sm:p-10"><div className="flex flex-wrap items-end justify-between gap-6"><div><p className="text-sm text-teal-100/65">{user.email}</p><h2 className="fn-display mt-2 text-4xl font-extrabold tracking-[-.05em]">{profile?.username??'Cleaner'}</h2><p className="mt-2 text-teal-100/75">{profile?BADGE[profile.badge_level]:'Scout'} · <strong className="text-gold-300">{profile?.total_impact_points??0} XP</strong></p></div><StatusChip tone="gold">Active citizen</StatusChip></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-teal-100/60">Reports submitted</p><p className="mt-2 text-3xl font-extrabold">{mapStats.reports}</p></div><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-teal-100/60">Hotspots cleared</p><p className="mt-2 text-3xl font-extrabold">{mapStats.clears}</p></div><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-teal-100/60">Impact points</p><p className="mt-2 text-3xl font-extrabold text-gold-300">{profile?.total_impact_points??0}</p></div></div><button type="button" onClick={()=>void signOut()} className="mt-7 text-sm text-teal-100/65 underline">Sign out</button></section><div className="grid gap-5 lg:grid-cols-2"><section className="rounded-[1.5rem] bg-white p-6 shadow-[var(--shadow-card)]"><SectionLabel>Community days</SectionLabel><h2 className="fn-display mt-3 text-2xl font-extrabold text-[#063b32]">Cleanups attended</h2>{rsvps.length===0?<p className="mt-5 text-sm text-[#617972]">No cleanups yet. <Link to={path('/cleanups')} className="font-bold text-[#0b8c76]">See weekly cleanups →</Link></p>:<ul className="mt-5 space-y-2">{rsvps.map(r=><li key={r.id} className="flex justify-between gap-3 rounded-xl bg-[#f3f7f4] p-3 text-sm"><span className="font-semibold text-[#36564e]">{(r.events as Event|null|undefined)?.title??'Cleanup'}<span className="ml-2 text-xs font-normal text-[#71867f]">{r.status}</span></span>{r.points_awarded>0&&<span className="font-bold text-[#087766]">+{r.points_awarded} XP</span>}</li>)}</ul>}</section><section className="rounded-[1.5rem] bg-white p-6 shadow-[var(--shadow-card)]"><SectionLabel>Season 2</SectionLabel><h2 className="fn-display mt-3 text-2xl font-extrabold text-[#063b32]">Trash Race tickets</h2>{tickets.length===0?<p className="mt-5 text-sm text-[#617972]">No ticket yet. <Link to={path('/race')} className="font-bold text-[#0b8c76]">Register free →</Link></p>:<div className="mt-5 space-y-4">{tickets.map(t=><RaceTicket3D key={t.id} code={t.ticket_code} holderName={t.full_name} teamName={t.team_name??undefined} compact/>)}</div>}<p className="mt-5 text-xs text-[#71867f]">Your verified activity will appear here as the ledger grows.</p></section></div></div>}</section></main><SiteFooter/></PublicShell>}
