import { useCallback, useEffect, useState } from 'react'
import { SiteFooter } from '../components/site/SiteNav'
import { ActionLink, PageIntro, PublicShell, SectionLabel, StatTile, StatusChip } from '../components/site/PagePrimitives'
import { loadLocalRaceHotspots } from '../lib/raceHotspots'
import { loadLocalRaceRegistrations } from '../lib/raceRegistration'
import { buildTeamLeaderboard, loadLocalRaceWeights, type HotspotPointCredit, type TeamLeaderboardRow } from '../lib/raceWeights'
import { isSupabaseConfigured, supabase } from '../lib/supabase'
import { AMAZING_TRASH_RACE_S2, type RaceWeightLog } from '../types/database'
function ticketCountsFromRegs(regs:{team_name:string|null}[]){const counts:Record<string,number>={}; for(const r of regs){const t=r.team_name?.trim()||'Unassigned';counts[t]=(counts[t]??0)+1} return counts}
function localHotspotCredits():HotspotPointCredit[]{return loadLocalRaceHotspots().filter(h=>h.status==='cleared').map(h=>({cleared_by_team_name:h.cleared_by_team_name,point_value:h.point_value}))}
export function RaceLeaderboardPage(){const[rows,setRows]=useState<TeamLeaderboardRow[]>([]);const[usingLocal,setUsingLocal]=useState(false);const[updatedAt,setUpdatedAt]=useState<string|null>(null);const refresh=useCallback(async()=>{let weights:RaceWeightLog[],ticketCounts:Record<string,number>,hotspotCredits:HotspotPointCredit[];if(!isSupabaseConfigured){setUsingLocal(true);weights=loadLocalRaceWeights();ticketCounts=ticketCountsFromRegs(loadLocalRaceRegistrations());hotspotCredits=localHotspotCredits()}else{const[{data:w,error},{data:regs,error:rErr},{data:cleared,error:hErr}]=await Promise.all([supabase.from('race_weight_logs').select('*').eq('event_slug',AMAZING_TRASH_RACE_S2),supabase.from('race_registrations').select('team_name').eq('event_slug',AMAZING_TRASH_RACE_S2),supabase.from('race_hotspots').select('cleared_by_team_name, point_value').eq('event_slug',AMAZING_TRASH_RACE_S2).eq('status','cleared')]);if(error||rErr||hErr){setUsingLocal(true);weights=loadLocalRaceWeights();ticketCounts=ticketCountsFromRegs(loadLocalRaceRegistrations());hotspotCredits=localHotspotCredits()}else{setUsingLocal(false);weights=(w??[]) as RaceWeightLog[];ticketCounts=ticketCountsFromRegs((regs??[]) as {team_name:string|null}[]);hotspotCredits=(cleared??[]) as HotspotPointCredit[]}}setRows(buildTeamLeaderboard(weights,ticketCounts,hotspotCredits));setUpdatedAt(new Date().toLocaleTimeString())},[]);useEffect(()=>{const boot=window.setTimeout(()=>void refresh(),0);const id=window.setInterval(()=>void refresh(),8000);return()=>{window.clearTimeout(boot);window.clearInterval(id)}},[refresh]);useEffect(()=>{if(!isSupabaseConfigured||usingLocal)return;const channel=supabase.channel(`race_weights_lb_${crypto.randomUUID()}`).on('postgres_changes',{event:'*',schema:'public',table:'race_weight_logs'},()=>void refresh()).on('postgres_changes',{event:'*',schema:'public',table:'race_hotspots'},()=>void refresh()).subscribe();return()=>{void supabase.removeChannel(channel)}},[refresh,usingLocal]);const totalPoints=rows.reduce((s,r)=>s+r.points,0);const totalKg=rows.reduce((s,r)=>s+r.kg,0);return <PublicShell><main><PageIntro eyebrow="Season 2 · live scoring" title="The board moves when the city moves." body="Teams earn points by clearing race hotspots. We keep kilograms visible for impact reporting, but the ranking follows verified points." action={<div className="flex flex-wrap gap-3"><ActionLink to="/race" tone="gold">Register a squad</ActionLink><ActionLink to="/race/marshal" tone="light">Marshal login</ActionLink></div>}/><section className="mx-auto max-w-7xl px-5 pb-12 sm:px-8 lg:px-12"><div className="grid gap-5 sm:grid-cols-3"><StatTile label="Squads on board" value={rows.length} detail="live standings"/><StatTile label="Total points" value={totalPoints} detail="verified hotspot credits" accent="teal"/><StatTile label="Logged weight" value={`${totalKg.toFixed(1)} kg`} detail={updatedAt?`updated ${updatedAt}`:'waiting for update'} accent="coral"/></div></section><section className="bg-[#063b32] px-5 py-16 text-white sm:px-8 lg:px-12"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><SectionLabel>Live standings</SectionLabel><h2 className="fn-display mt-3 text-4xl font-extrabold tracking-[-.05em]">Who is showing up?</h2></div>{usingLocal&&<StatusChip tone="gold">Local demo data</StatusChip>}</div><div className="mt-8 space-y-3">{rows.length===0&&<div className="rounded-[1.5rem] border border-white/15 bg-white/5 p-10 text-center text-sm text-teal-100/70">No squads on the board yet. Clear race hotspots and log kilograms at marshal checkpoints.</div>}{rows.map((row,i)=><article key={row.team} className={`grid grid-cols-[3rem_1fr_auto] items-center gap-4 rounded-[1.35rem] p-4 ${i===0?'bg-gold-400 text-[#063b32]':'bg-white/10 text-white'}`}><span className={`grid h-11 w-11 place-items-center rounded-xl text-xl font-extrabold ${i===0?'bg-[#063b32] text-gold-300':'bg-white/10 text-gold-300'}`}>{i+1}</span><div><p className="font-extrabold">{row.team}</p><p className={`mt-1 text-xs ${i===0?'text-[#38574f]':'text-teal-100/65'}`}>{row.kg.toFixed(1)} kg · {row.tickets} tickets · {row.logs} weigh-ins</p></div><div className="text-right"><p className="text-2xl font-extrabold">{row.points}</p><p className={`text-[10px] font-bold uppercase tracking-[.15em] ${i===0?'text-[#38574f]':'text-teal-100/55'}`}>points</p></div></article>)}</div></div></section></main><SiteFooter/></PublicShell>}
